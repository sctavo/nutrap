# nutrap
# 1. Instalar el SDK de Firebase
npm install firebase

# 2. Levantar el backend en Docker (el emulador de Firestore)
docker-compose up -d

# 3. Iniciar el servidor de desarrollo de React Native
npm start